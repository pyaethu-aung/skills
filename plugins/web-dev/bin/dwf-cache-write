#!/usr/bin/env bash
# web-dev plugin wrapper: runs the develop-web-feature helper script from the
# plugin root, so permission grants can be path-independent (Bash(dwf-cache-write*)).
exec node "$(dirname "$0")/../skills/develop-web-feature/scripts/cache-write.mjs" "$@"
